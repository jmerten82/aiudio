# Dev/test dependencies for the Phase-0 Python test suites (testing/README.md).
#   pip install -r testing/python/requirements-dev.txt
# (equivalent to `pip install -e ".[test]"`). The aiudio package itself is installed
# separately with `pip install .` (it builds the C++ core via scikit-build-core).
pytest>=8.0
ruff>=0.5
numpy>=1.26
nbformat>=5.0
nbclient>=0.9
jupyter-client>=8.0
ipykernel>=6.0
matplotlib>=3.7
